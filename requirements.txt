# Sprint 1 needs nothing beyond the standard library and a test runner.
# Sprint 2+ dependencies are added as each sprint lands, so a broken wheel
# never blocks work that does not need it.

pytest>=8.0

# --- MAVLink layer ---
pymavlink>=2.4.49

# --- Companion: capture, geotagging, image validation ---
piexif>=1.1.3
pillow>=10.0

# --- Local viewer (browse reconstructions, 3D and map views) ---
# The frontend has no build step: plain ES modules, three.js from a CDN.
fastapi>=0.115
uvicorn[standard]>=0.32

# picamera2 is Raspberry Pi only and is installed on the Pi, not here.
# The geotagging and validation code is deliberately hardware-independent so it
# can be developed and tested on any machine.

# --- Sprint 3: web UI / backend ---
# fastapi>=0.115
# uvicorn[standard]>=0.32
